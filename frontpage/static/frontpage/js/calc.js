// When #area input gets changed, make an AJAX request to
// a dedicated endpoint to fetch:
// - length
// - width

// When #plantspecies input gets changed, make an AJAX request to
// a dedicated endpoint to fetch:
// - LIST of varieties
// - default distb, distw, yield (and revenue)

// When #plantvariety input gets changed, make an AJAX request to a
// dedicated endpoint to fetch
// - default distb, distw, yield (and revenue)
// it has to keep the plantspecies parameter if any of the above is not
// available for the selected variety
